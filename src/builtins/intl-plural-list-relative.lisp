;;;; builtins/intl-plural-list-relative.lisp — Intl.PluralRules + Intl.ListFormat + Intl.RelativeTimeFormat.
(in-package #:shuttle)

(defun install-intl_plural_list_relative (realm)
  (declare (ignorable realm))
  ;; TODO(intl)
  )

(register-builtin-installer 'install-intl_plural_list_relative)

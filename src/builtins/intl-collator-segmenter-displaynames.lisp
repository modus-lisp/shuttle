;;;; builtins/intl-collator-segmenter-displaynames.lisp — Intl.Collator + Intl.Segmenter + Intl.DisplayNames + Intl.DurationFormat.
(in-package #:shuttle)

(defun install-intl_collator_segmenter_displaynames (realm)
  (declare (ignorable realm))
  ;; TODO(intl)
  )

(register-builtin-installer 'install-intl_collator_segmenter_displaynames)

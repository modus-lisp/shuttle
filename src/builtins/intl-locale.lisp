;;;; builtins/intl-locale.lisp — Intl.Locale.
(in-package #:shuttle)

(defun install-intl_locale (realm)
  (declare (ignorable realm))
  ;; TODO(intl)
  )

(register-builtin-installer 'install-intl_locale)
